gerando um arquivo de tempo de tabalho em log ou workspace 
<!--<!doctype class timeout>-->
{
    class text: log:{
       "log"= applet timeout=pc.moisesfdl }
    {
       public logIterator log
       {
           get
           {
               return new logIterator(this);
           }
       }
       
       public class logIterator
       {
           readonly timeout outer;
           
           internal logIterator(timeout outer)
           {
               this.outer = outer;
           }
           
           // TODO: provide an appropriate implementation here
           public int Length { get { return 1; } }
           
           public ElementType this[int index]
           {
               get
               {
                  
                   throw new System.NotImplementedException();
                   return default(ElementType);
               }
           }
           
           public System.Collections.Generic.IEnumerator<ElementType> GetEnumerator()
           {
               for (int i = 0; i < this.Length; i++)
               {
                   yield return this[i];
               }
           }
       }
    }
     return.log=[timeout]
}
--------------------------------------------------------
<!--<!doctype class timeout>-->
<!--<doctype class xml>-->
    <html lang="en">
    <title area="timeout.log">timeout</title>
    <head>
        <meta charset="UTF-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <meta http-equiv="X-UA-Compatible" content="ie=edge"/>
        <title name="CarteiraDeTrabalho" >Document</title>
        
    </head>
    <body>
    <!--@RGMoisesFerreiraDeLima-->
        <script applet="timeout.log">
        <applet area="timeout"pc.moisesfdl>
          </applet></script>
          <dir doctype=log href=cp.moisesfdl menssagBox.log>
          <area shape="applet" coords="timeout" href="log" alt="cp.moisesfdl"/>
          <article timeout=temp scale=log.Document log=doctype></article>
          </dir>
    </body>
    </html>
    ------------------------------------------------------------------------------------------
    

<!--<doctype class timeout>-->
<!--<!doctype class html>-->
<html lang="en">
<title aria-details="timeout.log">timeout</title>

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title name=CarteiraDeTrabalho aria-valuetext="log">Document</title>
    <meta aria-details="text.log" aria-colindex="log.log" inputmode="text" aria-modal="true">

    <head body="script"></head>
</head>

<body>

    <div>
        <area title="doctype" @RGMoisesFerreiraDeLima aria-checked="@RGMoisesFerreiraDeLima" aria-details="doctype" Registro></area>
        <a href="tel:(+5512)99649-7466" aria-details="titular" moises_ferreira_de_lima aria-controls="tel" 99649-7466></a>
        <img src="timeout.css" alt="">
    </div>
    <script applet="timeout.log">
        < applet console.log(timeout) status = "function".if index = "log" / > < /applet>
    </script>
    <div>
        <acronym title="timeout" inputmode="Desktop" aria-readonly="timeout.log" text></acronym>
    </div>
    <script class="if">
        {
            if (index.log | menssagBOX.class: index.log,
                array.length, index.log),
            const.element = array[index.log];
        }
        var {
            varlog = require.(
                return .log);
            tabindex = applet.timeout;
        }
    </script>

</body>

</html>
    
    
